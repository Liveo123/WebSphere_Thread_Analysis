WSThreadDump.sh is used to create two thread dumps of WebSphere (or whatever you configure) over a specified period.
analyse_td.pl is used to analyse the two dumps to extract the most useful information, including information about
threads common to both dumps.